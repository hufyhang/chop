chop.js responsive-design module

HTML classes
=============

`ch-row`
--------

Layout row.

`ch-[size]-[cols]`
------------------

`size` could be: `xs` for phones, `sm` for tablets, `md` for desktops, and `lg` for large desktops.

`cols` ranges from `0` to `12`.
